# Connection string do pooler de sessão do Supabase (Project Settings > Database
# > Connection string > Session pooler). Nunca commitar o .env real com essa
# variável preenchida.
SUPABASE_DB_URL=postgresql://usuario:senha@host:6543/postgres
