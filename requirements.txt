numpy>=1.26
psycopg2-binary>=2.9
streamlit>=1.38
pandas>=2.2
plotly>=5.24
requests>=2.32

# usado pelo terceiro estimador (rede neural, puramente comparativo, ver
# src/analise/aprendizado.py) no motor de análise e no dashboard. Sem
# ela instalada o pipeline principal continua funcionando: dano_ml vira
# NaN/NULL em vez de quebrar, ver estimar_dano_ml().
scikit-learn>=1.4
